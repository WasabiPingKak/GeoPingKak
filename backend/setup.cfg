[tool:pytest]
testpaths = tests
markers =
    emulator: requires Firestore Emulator running on localhost

[coverage:run]
source = .
omit =
    tests/*
    scripts/*

[coverage:report]
show_missing = true
fail_under = 90
